{
  "name": "chrome-tmux",
  "version": "0.1.0",
  "private": true,
  "description": "A tmux-style prefix and a vim mode for Chrome.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node --test 'test/*.test.mjs'"
  },
  "type": "module",
  "devDependencies": {
    "@types/chrome": "^0.2.5",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
