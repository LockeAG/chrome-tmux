{
  "name": "chrome-tmux",
  "version": "0.1.0",
  "private": true,
  "description": "A tmux-style prefix and a vim mode for Chrome.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node --test 'test/*.test.mjs'",
    "smoke": "playwright test",
    "build:store": "node tools/build-store.cjs"
  },
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/chrome": "^0.2.5",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  },
  "packageManager": "pnpm@10.29.3"
}
